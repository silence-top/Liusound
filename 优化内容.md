可以。核心原则是：主题不再是同一套玻璃 UI 换颜色，而是共享业务状态、各自拥有独立的“页面舞台、容器、导航、弹层、列表与动效规则”。

## 目标主题

| 主题 | 独立设计语言 | 禁止复用的液态玻璃元素 |
|---|---|---|
| 液态玻璃 | 深色流动背景、真实背景模糊、半透明胶囊、镜面高光、折射层次；保留 iOS 26 风格方向 | — |
| 深空科幻 | 墨蓝画布、星点/细网格、青紫霓虹边线、发光进度与播放态；以实色深层面板为主 | 模糊、白色镜面描边、玻璃光斑 |
| 极简纯色 | 暖黑/米白，高密度排版、细分隔线、低圆角、克制色块；强调内容而非装饰 | 模糊、渐变边框、阴影、光斑、持续动画 |
| Material You | Android 动态色完整驱动 `ColorScheme`，采用 tonal surface、M3 形状与状态层 | 固定深蓝背景、玻璃容器、硬编码白字 |
| 高对比无障碍 | 纯黑底、高亮白字、明确描边、大字号、强焦点环、最少动效 | 透明度传达信息、弱灰文字、模糊、发光 |

## 架构改造计划

### 1. 先建立主题契约

将现有 `SkinTokens` 扩展为两层：

- 语义 token：`canvas`、`surface`、`surfaceRaised`、`textPrimary`、`textSecondary`、`divider`、`brand`、`danger`、`focusRing`。
- 主题行为：`usesBackdropBlur`、`usesAmbientStage`、`cardVariant`、`navigationVariant`、`sheetVariant`、`motionProfile`、`typographyScale`。

删除组件层直接判断 `AppSkin` 或直接使用 `Colors.white` / 固定十六进制色；页面只消费语义 token。

### 2. 拆掉“全局 GlassSurface”

把当前 [glass.dart](D:/WorkSpace/CodeRoom/Others/music/music-app/lib/shared/widgets/glass.dart) 的职责拆成主题组件：

- `AppStage`：页面背景与主题独有装饰。
- `AppSurface`：卡片/列表/输入框容器。
- `AppNavigation`：底栏与顶部导航。
- `AppSheet`、`AppDialog`：弹层与确认框。
- `AppListEnd`：统一的列表结束状态。

液态玻璃实现可以继续用 `BackdropFilter`；其他主题必须走各自实现，不能仅传入 `blur: 0`。

### 3. 按主题分别落地

建议顺序：

1. 液态玻璃：先作为当前视觉的兼容基线，修正真正的层级、模糊范围与性能边界。
2. 极简纯色、高对比：两者最适合验证“没有玻璃组件也可正常工作”。
3. 深空科幻：落地独立舞台、霓虹焦点与播放反馈。
4. Material You：接入完整动态 `ColorScheme`，不只取 `primary`。
5. 最后迁移首页、资料库、详情页、播放器、设置页和所有弹层，逐页截图回归。

## 功能修复计划

### P0：本轮必须修复

- 桌面歌词首条文本写入原生浮窗；授权返回后重新检测权限并自动启用。
- 原生关闭浮窗时回传 Flutter，同步关闭设置状态，避免下一句歌词又重建。
- 播放失败时不提前更新当前歌曲；应在音源成功加载后提交状态，并给出可理解的失败提示。
- 修复“检测连接”始终检测当前服务器的问题。
- 给 scrobble 队列增加 `server_id`，迁移旧表，并按当前服务器分别补发。

### P1：紧随其后

- 列表触底拆成两项：
  - “列表结束提示”：每个歌曲列表都插入，文案上下文取当前列表，不取全局播放队列。
  - “加载更多”：仅数据源支持分页时出现，具备 loading、失败重试、无更多数据状态。
- 修复曲库快照：版本检查失败时优先返回已有快照；仅无快照时请求网络，并避免失败后重复请求。
- 离线下载改用稳定的 `songId` 文件名与本地索引，不用“歌手 - 标题”猜测文件。
- 将“移动网络传输”改为真正的请求门禁，播放、下载和自动下载全部统一拦截。
- 自动下载改为：启动、收藏变化、网络从非 Wi‑Fi 变为 Wi‑Fi 时均可补跑。

### P2：设置与状态一致性

- 所有偏好设置统一改为异步初始化状态，避免启动读取旧值覆盖用户刚刚选择的新值。
- 搜索状态改为页面级 `autoDispose`，重新进入搜索页时输入框与结果一致。
- 设置项图标开关覆盖音量、版本等所有设置行，或者调整文案为“部分设置项图标”。
- 液态玻璃档位、自定义背景等仅在支持的主题显示；其余主题改为对应的主题专属设置。

## 设置页重组

从当前单一大页面拆成四组，且每项都建立“设置值 → 实际消费者 → 验证方式”的映射：

- 播放：循环、速度、淡入淡出、定时、音效、耳机线控、音量。
- 网络与缓存：Wi‑Fi/蜂窝音质、传输限制、代理、缓存、下载、缓存清理。
- 外观：主题选择、主题专属设置、封面样式、字体/密度、减少动效。
- 系统与账户：桌面歌词、服务器、版本、退出登录。

主题选择器应改成预览卡网格；选择某主题后，设置页本身也立即切为该主题，不能继续使用玻璃底部弹层。

## 验收与测试

每个主题至少验证：首页、资料库、歌曲/专辑详情、全屏播放器、搜索、设置、底部弹层、错误/空列表。

新增测试覆盖：

- 悬浮歌词授权、首句、关闭与撤权。
- 服务器切换、连接检测、scrobble 隔离。
- 播放加载失败后的状态回滚。
- 弱网曲库快照回退。
- 蜂窝网络禁传。
- 下载同名歌曲不覆盖。
- 主题切换后各主题组件树与设置项可见性。

建议分三期实施：第一期先完成 P0 与主题基础组件；第二期完成五套主题和页面迁移；第三期补分页、下载/缓存、测试与真机验收。