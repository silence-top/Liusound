# 待办清单：未实现功能

> 来源：《修改内容.md》附录「全量功能清单」及正文遗留项核对结果（2026-09-04）。  
> 2026-09-04 全量执行完毕（Phase A–I），已实现项已勾选；所有行为级功能（播放/主题/缓存/EQ/上报/Widget 等）**均未经真机验收**，验收清单见文末。  
> 按 P0 → P2 分组，价值与依赖程度综合排序。

## P0 — 高价值、依赖轻，建议先做

- [x] **在线音质分档设置**：移动网络 / Wi-Fi 独立配置（无损 / 320k / 256k / 192k / 128k），转码格式 MP3 / OPUS（附录·四）`待真机验收`
- [x] **缓存管理**：边听边存、自动下载、自定义限额（2GB/5GB/10GB/无限制）、一键清理（附录·四）`待真机验收`
- [x] **移动网络传输开关**（附录·四，与音质设置同一屏）`待真机验收`
- [x] **歌词手动导入**：服务端未匹配 LRC 时手动导入本地 .lrc 文本（附录·六；第三方 API 抓取补偿已规划到「后续插件系统」）`待真机验收`
- [x] **网络与代理设置**：自定义超时、代理、DNS、HTTPS 证书校验开关（附录·一）`待真机验收`
- [x] **双击手势组**：双击封面收藏、双击歌词切翻译/跳下一句（附录·七）`待真机验收`
  - 列表图标跳队列由现有单击行为覆盖（队列入口常驻播放页），未重复加双击；上滑唤出全屏播放器由 mini player 点击展开覆盖
- [x] **自动打开播放页**开关（附录·三）`待真机验收`
- [x] **Crossfade（0–10s 可调）与 Gapless 无缝播放**（附录·五）`待真机验收`
  - just_audio 无原生 crossfade，采用音量自动化（距尾渐出 + 下一首渐入）；Gapless 用 ConcatenatingAudioSource 连续队列

## P1 — 依赖中等、需要设计

- [x] **Scrobble 实况上报**：播放进度达 50% 或满 2 分钟时上报 Play Count 与 Now Playing（附录·二）`待真机验收`
  - 能力矩阵：Subsonic/Navidrome `scrobble`、Jellyfin/Emby `/Sessions/Playing`、Plex `/:/scrobble`、Audio Station 无接口如实降级
- [x] **离线上报队列**：断网时播放记录暂存本地 SQLite，网络恢复后按时间戳补发（附录·二）`待真机验收`
- [x] **增量元数据同步**：服务端库校验（lastModified / DateLastMediaAdded / updatedAt）与本地快照比对，仅拉变动部分（附录·二）`待真机验收`
  - Audio Station 不支持校验值 → 如实全量刷新
- [x] **10/31 段图形 EQ**：内置预设（平直/流行/摇滚/古典/人声/电子），自定义曲线剪贴板导入/导出（附录·五）`待真机验收`
  - **仅 Android**：Equalizer/BassBoost/Virtualizer 挂接 just_audio 的 audioSessionId；iOS 不可行原因见文末说明
- [x] **歌词结束提示文案**：支持 {nTitle} / {nArtist} / {nAlbum} 变量占位符（正文 §8.4）
- [x] **省电模式限制高刷新率**：Android `flutter_displaymode` 按省电开关切低帧率（正文 §8.5 遗留，模糊与动画压缩此前已实现）`待真机验收`
  - iOS 无帧率控制 API，该项自动跳过
- [x] **主题系统化**（正文 §8.1 遗留）：液态玻璃抽离为可切换主题，共 5 套 + 六色 accent 全局可切 `待真机验收`
  - **主题架构改造**：GlassTokens static const（59 处）与 AppTheme 静态色板（47 处）已改为每主题一份 SkinTokens ThemeExtension 配置；整树重建由 main.dart watch skin+accent 接线保证（glass_quality 非响应式问题随之解决）
  - **液态玻璃主题（渐进升级）**：方向性镜面高光描边 + 内容透色自适应 tint + 悬浮层叠感增强（未做 shader 边缘折射，按确认范围）
  - **新主题**：深空科幻（近黑蓝底 + 霓虹青/品红 + 发光描边）、极简纯色（无模糊实色卡片强排版）、Material You 动态取色（DynamicColorBuilder，accent 可手动覆盖）、高对比无障碍（白字纯黑底 ≥7:1、去模糊去发光、焦点描边加粗）
  - **白阶语义化（232 处）说明**：本轮所有主题均为深色系，Colors.white* 阶在全部主题下视觉有效，故未强制替换为语义 token；若后续新增浅色主题需先补此步
- [x] **悬浮歌词**（附录·七）：Android 悬浮窗显示当前行、可拖动/关闭；iOS 不支持悬浮窗 → 能力隐藏
- [x] **长音频独立断点**（附录·三）：>10min 曲目单独持久化进度，冷启动提示续播 `待真机验收`

## P2 — 平台特定、依赖重

- [x] **本地 ID3 内嵌元数据解析**：读取 FLAC/MP3/M4A 标签、嵌入封面与内嵌 LRC（附录·六）
  - 链路已通：离线文件回放（`local:` 前缀 + 已下载文件反查）→ 本地音乐页真扫描（Android /Music 与 /Download、Windows ~/Music）→ audio_metadata_reader 解析标签/封面/内嵌歌词入库
  - **依赖替换说明**：原计划的 audiotags 包需 Rust/cargokit 工具链（本机无 Rust），已换为纯 Dart 的 audio_metadata_reader 1.7.1
  - 注意：下载文件反查依赖文件名前缀匹配，服务端设置 Content-Disposition 时可能漏检（已知限制）`待真机验收`
- [x] **空间音频**：Android 立体声扩展近似（Virtualizer）+ iOS 混响近似，标注「近似」（附录·五）`待真机验收`
  - 3D 环绕的完整头部传递函数建模客户端不可行，为效果近似
- [x] **ReplayGain 响度平滑**：**未实现**（原因见文末：本地标签解析库未暴露响度标签）
- [x] **桌面 Widget**：4×2 播放控制小部件（Android home_widget，Kotlin 侧 HomeWidgetPreferences 直读 + BYPASS 键值绕过线控映射）
  - 2×2 布局与 iOS WidgetKit 版本未做，可在验收后按需追加
- [x] **CarPlay & Android Auto**：Android Auto 经 audio_service MediaSession 出现于车机，支持队列浏览 + 播放控制
  - 完整曲库浏览需升级 MediaLibraryService（getChildren 目前仅返回当前队列），列为后续项；CarPlay 需 ActivityKit 级原生改造，见文末说明
- [x] **AVRCP 元数据推送**：audio_service MediaSession 天然推送标题/歌手/封面/进度到蓝牙耳机与车机（封面经 artHeaders 带鉴权头拉取）
- [x] **线控按键自定义映射**：单击/双击/三击功能配置（播放暂停/下一首/上一首/收藏），Dart 侧 400ms 聚合窗口实现
  - 长按与音量键由系统控制，不可自定义；桌面 Widget 的播放/暂停用 BYPASS 键值绕过用户映射（避免误触自定义动作）

## 正文遗留的有意偏差（维持不做）

- [x] §4.3 非当前歌词行「轻微高斯模糊」→ 当前用字号/字重/透明度景深衰减替代；逐行 ImageFiltered 在滚动列表内会产生 N 次 saveLayer，违反 §2.2 性能红线，**维持不补做**

## 后续插件系统（暂不实现）

以下功能依赖第三方网络数据源，统一规划到插件系统中实现：

- [ ] **多源歌词补偿 API**：按标题+歌手向第三方歌词服务（网易云/QQ 音乐等）抓取补全 LRC（附录·六；手动导入 .lrc 已完成，见 P0）
- [ ] **歌词罗马音/拼音「音」开关**（正文 §4.3 遗留）：六个后端均无对应数据源，需插件提供翻译/音译数据源后接入歌词页徽章位

---

## 不可行 / 降级项如实记录（本次不实现的原因）

| 项 | 结论 | 原因 |
|---|---|---|
| 人声分离 / 动态低音增强 | 不实现 | 客户端无可行方案：需独立 ML 模型推理（源分离网络），Flutter 侧无成熟推理管线，强行塞模型会带来数百 MB 体积与不可接受的延迟 |
| Bit-perfect 硬件直通独占输出 | 不实现 | just_audio（ExoPlayer/AVPlayer 封装）不暴露独占音频会话接口；独占输出需绕过系统混音器自建 AudioTrack/AVAudioEngine 直通管线，与现有播放内核架构冲突 |
| iOS EQ / 低音增强 / 空间音效（iOS 端） | 降级为 Android-only | just_audio iOS 端不暴露 AVAudioEngine 管线，无法挂载 AVAudioUnitEQ；iOS 设置面板显示能力不可用提示 |
| ReplayGain 响度平滑 | 暂不实现 | audio_metadata_reader 1.7.1 未暴露 RG_INFO/Loudness 标签读取；流媒体侧各后端也不提供响度元数据。待标签库支持或插件系统再评估 |
| CarPlay | 尽力而为，未实现 | 需 CarPlay ActivityKit 级原生改造（列表模板 + 媒体会话桥接），工作量等同独立原生模块；现有 audio_service 会话可被 CarPlay 识别基础播放，但无浏览模板 |
| iOS Live Activities / 动态岛 | 仅记录设计要点 | 设计要点：ActivityKit `Activity<NowPlayingAttributes>` + 锁屏/灵动岛 Live Activity，经 iOS 平台通道从 Dart 推送 playing/title/artist/position，依赖 WidgetKit 基础；建议作为独立后续项（需原生 Swift 代码与实时更新通道） |
| Android Auto 完整曲库浏览 | 部分实现 | 当前 getChildren('root') 返回当前队列；完整曲库浏览需迁移到 MediaLibraryService 并实现分层内容树，列为后续项 |
| 悬浮歌词 iOS | 能力隐藏 | iOS 无第三方 App 悬浮窗 API（仅画中画），设置项在 iOS 自动隐藏 |
| 省电限帧 iOS | 自动跳过 | iOS 无帧率控制 API |

## 真机验收清单（analyze/build 只证明可编译，以下全部待真机验收）

1. 音质分档：Wi-Fi / 蜂窝各自生效，蜂窝开关关闭时移动网络不播放流
2. 缓存：边听边存命中后离线可播；限额超限 LRU 清理；一键清理后占用归零
3. 歌词导入：无匹配歌曲出现「导入歌词」入口 → 粘贴 LRC → 重新进入歌词页命中
4. 网络设置：超时/代理/证书校验开关对六个后端请求生效
5. 手势：双击封面收藏（含回滚）、双击歌词切翻译、mini player 上滑全屏
6. Crossfade / Gapless：连播过渡听感、设置滑杆生效
7. Scrobble：50%/2min 触发上报；断网入队、恢复补发
8. EQ：预设/逐段滑杆/低音/空间音效出声；蓝牙断连重连后配置自动重挂
9. 主题：5 套切换整树重建无残留；Material You 跟随壁纸；高对比主题对比度
10. 悬浮歌词：悬浮窗显示/拖动/关闭；权限拒绝引导
11. 长音频：>10min 断点冷启动续播提示
12. 本地音乐：扫描入库、标签/封面/内嵌歌词解析、点击直播本地文件
13. 线控：单击/双击/三击按映射执行；桌面 Widget 三键 + 状态刷新
14. Android Auto：车机出现应用、播放/暂停/队列浏览
15. 省电模式：开启后帧率降档
