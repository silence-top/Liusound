# UI/UX 视觉与架构优化方案（合并版）

---

## 一、导航与布局重构

### 1.1 顶部导航冗余消除
- **问题**：首页既在顶部放置“搜索图标”，又在“下午好”下方放置大尺寸搜索框，功能重叠。
- **方案**：
  - 将顶部胶囊式 NavigationBar 迁移为底部毛玻璃 **Floating Bottom Navigation Bar**（含 歌曲/首页、资料库、设置 三 Tab），释放顶部空间。
  - 首页仅保留顶部胶囊态搜索框，点击进入全局搜索页，同一视图不出现两个搜索入口。

### 1.2 内嵌搜索栏收纳（歌单/专辑详情页）
- 列表顶部“搜索歌曲/专辑/歌手”输入框占用 48dp 纵向空间。
- **方案**：改为点击顶部 Header 搜索图标下拉展开，或在列表向上滚动时自动收起折叠，增大歌单显示区域。

---

## 二、液态玻璃（Glassmorphism）质感与层级补强

### 2.1 质感缺失修复
- 当前卡片仅呈现半透明暗蓝色 tint，缺乏渐变边框、高光边缘及背景高斯模糊。
- **方案**：
  - 对顶部“服务器卡片”及“每日推荐卡片组”应用 `GlassContainer`：
    - 1px 微亮边框：`Colors.white.withOpacity(0.12)`
    - 背景模糊：12px BackdropFilter
    - 增强浮空层叠感

### 2.2 性能红线遵循
- 列表滚动项（每日推荐列表、最近播放列表）**内部禁止挂载 BackdropFilter**，仅使用 tint 色值叠加，保障 Impeller 渲染引擎在 60/120fps 下无掉帧。

### 2.3 液态玻璃质量调节
- 提供 **增强（更通透）/ 标准 / 低性能降级** 三档效果，低端设备自动关闭高斯模糊。

---

## 三、歌单与专辑详情页

### 3.1 音质/码率 Label 视觉微调
- **现状**：`flac 955K` 纯色椭圆 Tag 样式偏重，占用视觉焦点。
- **方案**：
  - 更换为**高透玻璃胶囊样式**
  - 增加**音质色彩分级**：
    - **24bit / Hi-Res FLAC**：金色/琥珀色高光边框（如 `flac 24bit/96kHz`）
    - **16bit 无损 FLAC**：主题天蓝色微亮边框（如 `flac 1030K`）
    - **AAC / MP3 转码**：中灰半透明低调展示

### 3.2 批量操作与多选
- Header 栏增加 **“批量选择”** 入口，支持：
  - 批量勾选歌曲
  - 批量下载
  - 批量添加至歌单
  - 批量插入下一首播放

---

## 四、播放器页

### 4.1 “推荐”Tab 能力降级处理
- **适配规避**：Jellyfin/Emby/Plex 不支持“相似歌曲”与“星级评分”。
- **方案**：
  - 当检测到当前服务器不支持相似推荐时：
    - 自动隐藏“推荐”Tab，或
    - 替换为 **“歌手简介 / 专辑简介”**，避免视觉冗余与空洞的“暂无数据”

### 4.2 黑胶唱片视觉动态增强
- **现状**：黑胶封面质感偏平淡。
- **方案**：
  - 播放时黑胶右上角加入**唱针动画**（唱针平滑降至唱片；暂停/切歌时抬起）
  - 唱片外观支持在“个性化设置”中切换形态：
    - 经典黑胶（含唱针动画）
    - CD 唱片
    - 方形玻璃卡片
    - 全屏模糊大图

### 4.3 歌词页聚焦与交互优化
- **视觉景深**：
  - 非当前播放行歌词加入**轻微高斯模糊 + 透明度渐变**，突出高亮行（当前句），增强纵深感
- **点击行跳转**：
  - 点击任意非当前歌词行时，右侧显示小型 **Play 按钮 + 时间戳**，点击直接跳播至该句
- **翻译与音译开关**：
  - 歌词页左下角增加 **译（双语翻译）** 与 **音（罗马音/拼音）** 的快速切换 Toggle（位于 LRC 标识旁）

---

## 五、播放队列与歌曲操作弹窗

### 5.1 播放队列状态增强
- **当前播放项高亮**：
  - 序号处替换为**动态音频波形动画**
  - 该行加上**玻璃高亮背景色**
- **手势移除**：支持列表项**向左滑动**直接移除，保留右侧 X 按钮

### 5.2 “更多”操作弹窗层级与能力适配
- **星级评分自适应**：顶部五角星评分在 Jellyfin/Emby 下**自动隐藏**
- **金字塔式网格布局**：
  - 功能图标网格（下一首播放、添加到、下载、删除文件、定时停止、播放速度）
  - 封装为带**微光边框的 GlassCard**，提高点击触控反馈
- **歌曲信息展开**：
  - 底部“歌曲信息”栏点击展开弹窗，展示完整音频元数据：
    - 解码器
    - 采样率
    - 位深
    - 比特率
    - 本地磁盘缓存路径

---

## 六、首页与列表视觉细节优化

### 6.1 文本截断与排版
- **现状**：最新专辑卡片标题与副标题截断粗暴（如“我的楼兰（2026新...”）
- **方案**：
  - 使用**双行文本控件**，调整字号至 12sp
  - 选中时支持 **Marquee 跑马灯滚动**

### 6.2 播放操作热区
- 每日推荐与最近播放右侧圆圈播放按钮偏小
- **方案**：触摸热区提升至 **44×44 dp**，或支持：
  - 点击整行播放
  - 长按唤出上下文菜单

---

## 七、资料库与空状态交互

### 7.1 快捷入口对齐
- 资料库顶部四块快捷入口（歌曲、我喜欢的、本地音乐、专辑）图标尺寸与内边距不一致
- **方案**：统一使用网格布局（2×2 或 1×4 均分栏），卡片内采用**微玻璃材质**

### 7.2 空状态引导
- “暂无歌单”提示过于单薄
- **方案**：加入玻璃质感图标与 **“新建歌单 / 从服务器同步”** 主按钮，提供明确操作引导

---

## 八、全局视觉配置与个性化

### 8.1 主题与外观
- **主题类型**与**主题色切换**（粉色、蓝色、极夜黑等 Material 3 调色板）
- **自定义背景图**导入与不透明度/模糊度调节

### 8.2 控制栏定制
- 控制栏样式：纯色 / 毛玻璃 / 渐变
- 高度偏移微调

### 8.3 设置项图标显隐开关

### 8.4 自定义文本与变量
- **歌词结束提示文案**：支持变量占位符：
  - `{nTitle}` 下一首曲名
  - `{nArtist}` 下一首歌手名
  - `{nAlbum}` 下一首专辑名
- **列表触底文案**：自定义“到底啦”提示，或集成“一言 API”动态拉取句子

### 8.5 系统性能优化
- **省电模式开关**：限制高刷新率渲染与复杂模糊，降低 CPU/GPU 负载

---

## 附录：全量功能清单

### 一、多后端与服务架构
- 多协议适配：Navidrome、Subsonic、Jellyfin、Emby、Plex、Synology Audio Station
- **能力自适应矩阵**：根据后端能力自动显隐（星级评分、相似歌曲等）
- 多服务器管理：添加/切换/删除，凭证 `flutter_secure_storage` 加密存储，冷启动自动恢复会话
- 网络与代理设置：自定义超时、代理、DNS、HTTPS 证书校验开关

### 二、端云数据闭环与同步
- **听歌历史上报与 Scrobble 机制**：
  - 实况上报：播放进度达 50% 或播放满 2 分钟时，向后端 API（Subsonic/Navidrome/Jellyfin）上报 Play Count 与 Now Playing 状态
  - 离线上报队列：断网状态下的播放记录暂存本地 SQLite，网络恢复后按时间戳补发，确保服务端统计数据不丢失
- **增量元数据同步**：
  - ETag / 时间戳校验：万首级大曲库初始化后，后续仅拉取服务端 lastModified 变动的专辑与歌词，避免全量刷新的 API 性能瓶颈

### 三、播放内核与队列管理
- 事件驱动内核：`just_audio` 事件流 + `audio_service`（通知栏/锁屏/线控）
- 播放模式：顺序 / 列表循环 / 单曲循环 / 随机
- 播放队列：拖拽重排、左滑移除、一键清空、点击跳播、插入下一首
- 状态持久化：冷启动恢复上次队列与进度（前 100 首防抖落盘）
- 高级控制：启动后自动播放、自动打开播放页、音频焦点混音、睡眠定时器、0.5x–2.0x 变速、长音频独立断点

### 四、音质、传输与缓存
- 移动网络传输开关
- 在线音质：移动网络 / Wi-Fi 独立配置（无损 / 320k / 256k / 192k / 128k），转码格式 MP3 / OPUS
- 缓存管理：边听边存、自动下载、自定义限额（2GB/5GB/10GB/无限制）、一键清理

### 五、高级音效与 DSP
- 10/31 段图形 EQ，内置预设，导出/导入自定义曲线
- 空间音频（3D 环绕 + 混响）
- 动态低音增强、人声分离与高频增强、ReplayGain 响度平滑
- Crossfade（0–10s 可调）与 Gapless 无缝播放
- 硬件直通独占输出（Bit-perfect）

### 六、歌词与元数据容灾
- **多源歌词补偿**：
  - 当 self-hosted 服务端未匹配到 LRC 歌词时，支持按 **标题 + 歌手** 自动向第三方 API（如网易云/QQ音乐）发起抓取补全，或允许手动导入本地 .lrc 文本
- **本地 ID3 嵌入元数据解析**：
  - 在离线模式或播放本地音频文件时，使用底层 C/C++ 库直接读取音频文件（FLAC/MP3/M4A）内置的 ID3 标签、嵌入封面与内嵌 LRC 歌词

### 七、歌词与交互控制
- JSON/LRC 解析，毫秒级二分查找 + 平滑滚动
- 歌词微调 ±0.05s，单曲持久化
- 多渠道渲染：通知栏 / 悬浮 / 状态栏歌词
- 手势：双击封面收藏、双击歌词切换翻译/跳转下一句、双击列表图标跳转播放列表、上滑唤出全屏播放器

### 八、系统硬件联动与跨屏生态
- **CarPlay & Android Auto**：提供针对车载中控的防误触大字体布局与极简控制链条
- **iOS Live Activities / 动态岛**：全屏播放器退至后台时，在动态岛展示实时波形、封面图与播放进度
- **Android / iOS 桌面 Widget**：提供 2×2、4×2 多种规格的桌面播放控制与歌词小部件
- **AVRCP 元数据推送**：将专辑封面与 PCM 进度精准推送到蓝牙耳机/车载仪表盘屏幕
- **音频焦点优雅降音**：导航或系统通知响铃时，动态降低当前音乐音量（而非直接中断），通知结束后平滑恢复
- **物理按键/线控自定义**：单击/双击/三击耳机线控按键的功能映射（如双击切歌、长按唤起语音控制）

### 九、音乐库与多维浏览
- 首页：最新专辑、最近播放、最常播放、随机专辑、每日推荐（随机 50 首），下拉刷新
- 资料库：单曲 / 我喜欢的 / 本地音乐 / 专辑 / 歌手 / 歌单
- 全局搜索：300ms 防抖，歌曲/专辑/歌手分区展示，点击即播