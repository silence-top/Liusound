name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          # 与开发机一致（3.47.2 / Dart 3.13.2）；不锁版本时 stable 升级会
          # 连带 dart format 规则变化，导致 Format check 与本地结果背离
          flutter-version: 3.47.2
          cache: true

      - name: Pub get
        run: flutter pub get

      # test/ 不入库（2026-09-11 政策），闸门为格式 + 静态分析 + 凭据扫描
      - name: Format check
        run: dart format --output=none --set-exit-if-changed lib

      - name: Analyze
        run: flutter analyze --fatal-infos

      - name: Secret scan
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
